local _, ns = ...

-- Fixed defaults. There are no player-facing options; disable the AddOn to opt out.
ns.Defaults = {
  enabled = true,
  playerFrame = true,
  targetFrame = true,
  petFrame = true,
  partyFrames = true,
  castBars = true,
  minimap = true,
  objectiveTracker = true,
  classicTextures = true,
  classicComboPoints = true,
  hideLevelAlert = true,
}
